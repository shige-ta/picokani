## Public Release Note (2026-02-19)

This directory is prepared for GitHub publication from a private development environment.

Sanitization policy:

- Removed local build artifacts and logs (`build/`, `gateway.log`)
- Removed runtime local config (`config/config.json`)
- Removed agent/profile files (`SOUL.md`, `AGENT.md`, `USER.md`, `IDENTITY.md`)
- Removed skill definition files (`SKILL.md`, `skill.md`)
- Updated `.gitignore` to prevent re-inclusion of the above

No commit or push was performed.
